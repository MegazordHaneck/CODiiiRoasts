# Local sandbox — production values go in Amplify Console (backend + frontend env)
OPENAI_API_KEY=
# Optional — /admin only. If unset, /admin uses VITE_NSFW_PIN below.
VITE_ADMIN_PIN=
# Booth staff PIN: mean mode on intensity screen + /admin when VITE_ADMIN_PIN is unset
VITE_NSFW_PIN=1818
NSFW_PIN=1818
TTS_VOICE_ID=nova
ROAST_PROVIDER=openai
# Public URL of the deployed booth (used in share emails + links)
APP_ORIGIN=https://main.draz2nrbxwj96.amplifyapp.com
# Verified SES sender for roast card emails (verify in AWS SES once)
SHARE_FROM_EMAIL=roasts@yourdomain.com
SES_REGION=ca-central-1
